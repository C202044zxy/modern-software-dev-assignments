"""A small MCP server that wraps the Open-Meteo weather API.

You implement the modules listed below; ``types.py`` and ``main.py`` are
provided.

Modules:
    types     -- shared dataclasses + ToolError exception (provided)
    client    -- HTTP wrapper around Open-Meteo (Part 2)
    tools     -- tool schemas and call dispatch (Part 3)
    server    -- MCP server wiring (Part 4)
    main      -- STDIO entrypoint (provided)
"""
